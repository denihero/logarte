name: example
description: Example project for package:logarte
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  dio: ^5.3.0
  flutter:
    sdk: flutter
  json_explorer: ^0.1.2
  logarte: 
    path: ../
  markdown_widget: ^2.3.2+8
  provider: ^6.1.5+1
  share_plus: ^7.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true