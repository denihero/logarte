name: logarte
description: In-app debug console and logger for Flutter to monitor the app on-air
version: 0.2.12
repository: https://github.com/kamranbekirovyz/logarte
issue_tracker: https://github.com/kamranbekirovyz/logarte/issues
homepage: https://bio.kamranbekirov.com
topics: [logger, debugger, logging]

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.9.0
  flutter:
    sdk: flutter
  stack_trace: ^1.12.1
  json_explorer: ^0.1.2
  markdown_widget: ^2.3.2+8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: